name: hello-world

on: push

jobs:
   hello-world-job:

      runs-on: ubuntu-latest


      steps:
         -name: check put repo
          uses: actions/checkout@v4
         -run: echo "Hello world by git hub actions"
